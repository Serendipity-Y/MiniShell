# 安全政策

## 支持范围

当前维护中的 MiniShell 仅支持 SSH 与 SFTP。

## 报告漏洞

请不要在公开 Issue 中提交密码、私钥、服务器地址或可复现真实环境的连接信息。

在项目启用专用安全联系方式前，请通过仓库维护者的私下联系方式报告漏洞，并提供：

- 受影响的 MiniShell 版本；
- 不含真实凭据的复现步骤；
- 影响说明；
- 可选的修复建议。

## 主机密钥校验

MiniShell 使用“首次信任”（TOFU）校验 SSH 服务器主机密钥。首次连接时会展示 SHA-256 指纹，用户确认后才会建立连接；之后若同一主机与端口返回不同密钥，连接会被拒绝。

首次信任本身不能替代独立的身份核验。请从服务器管理员、可信运维文档或其他可信渠道核对指纹后再确认。
