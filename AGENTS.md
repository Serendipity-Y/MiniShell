# MiniShell 项目规则

## 产品边界

MiniShell 是一个极简、个人使用的 macOS 原生 SSH/Telnet 客户端，不以复刻 Xshell 为目标。

仅交付以下能力：

1. 保存 SSH/Telnet 连接配置。
2. 提供图形化 SSH/Telnet 终端。
3. SSH 会话顶部提供“文件传输”按钮。
4. 文件传输复用当前 SSH 会话的 Host、Port、Username 与凭据，自动建立 SFTP。
5. 提供本地/远程双栏文件管理。
6. 支持上传和下载。

技术基线：macOS 原生、Swift、SwiftUI；仅在必要处使用 AppKit。终端模拟使用 SwiftTerm，SSH/SFTP 使用 Citadel/NIOSSH，Telnet 使用 Network.framework，密码使用 Security.framework Keychain，异步逻辑使用 Swift Concurrency。

不得自行实现 SSH、SFTP 或终端模拟器。

## 永久排除项

未经用户明确改变产品范围，不得主动实现、引入或规划以下能力：

- AI、MCP
- RDP、VNC、Serial、FTP、WebDAV
- 云同步、用户账号系统、插件系统
- 代码编辑器、Docker 管理、Kubernetes、数据库客户端、服务器资源监控、Git 客户端
- Snippet、Macro、团队协作
- Telemetry、广告

## 实施原则

1. 从第一性原理确认功能是否服务于已定义的产品目标；不要把惯例、猜测或“未来可能需要”当作需求。
2. 每次实现前进行对抗式审查：验证前提、失败路径、边界条件、安全性、兼容性、可维护性和更简单的替代方案。
3. 优先最小、可验证、可回滚的实现；禁止为未来扩展预先过度设计。
4. 新问题先定位根因。不得用大规模重构、无关配置修改或功能扩张掩盖问题。
5. 当前任务未授权的功能、依赖、架构调整和范围扩张不得擅自执行；信息不足时先用代码、日志、配置或运行结果调查。
6. 区分已确认事实、高可信判断和未验证假设；环境相关结论必须说明验证范围。

## 安全与凭据

1. 密码及其他秘密只可保存于 Keychain。
2. 严禁将密码或秘密写入日志、JSON、UserDefaults、源码、测试夹具、截图或错误上报内容。
3. 输出诊断信息前先审查是否会暴露 Host、用户名以外的敏感连接数据；默认脱敏。
4. 文件传输必须仅使用当前 SSH 会话的连接参数与凭据建立 SFTP，不得创建旁路凭据存储。

## 任务流程与验收

1. 开始实现前：明确本次目标、范围、已知事实、待验证假设、最小方案和验收条件。
2. 修改前：说明修改对应的根因假设，以及失败时的回滚或恢复方式。
3. 修改后：在条件允许时实际编译、运行或测试；不得仅以“理论可行”作为结论。若未能实际验证，说明原因与未覆盖范围。
4. 每个开发阶段必须定义并完成明确、可执行的验收条件后，才进入下一阶段。
5. 保持改动聚焦；不混入格式化、重命名或无关重构。

## 每次任务结束的固定报告

必须按以下标题报告，未发生的项目也要明确说明：

【完成内容】
【修改文件】
【实际验证】
【发现的问题】
【风险】
【下一步最小行动】
